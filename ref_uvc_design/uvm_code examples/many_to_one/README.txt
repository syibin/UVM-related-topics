This example shows how to implement layering with sequences.

In order to run the example you should have the following environment 
variables set up:

QUESTA_HOME - Pointing to your Questa install

To compile and run the simulation, please use the make file:

make

After compilation you can run the simulation on its own:

make run

