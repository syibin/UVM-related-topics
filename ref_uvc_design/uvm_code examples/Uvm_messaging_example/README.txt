The messaging example shows the UVM messaging example in use

To run use the Makefile provided having first set up Questa

make all 

If you run the Makefile with the high_verbosity target, the test will run, but 
you will see differences in the number of messages displayed - there will be
more "green_id" messages.

